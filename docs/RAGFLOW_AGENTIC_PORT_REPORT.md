# Agentic RAG port report

The coherent pinned executor is implemented, not just helper functions. Deterministic tests execute low, medium, high and ultra; assert actual retrieval, native tool research, fanout preparation and sufficiency review; and reject unsupported mode labels and unauthorized identifiers. A separate 13-symbol AST gate compares critical algorithm bodies against exact upstream Git blobs, ignoring only imports/docstrings.

The test provider is a deterministic transport fixture, never Gemini. Offline success is not a live provider-compatibility or answer-quality claim. Live status is recorded in `RAGFLOW_FULL_PORT_LIVE_REPORT.md` after the freeze; until then it is NOT RUN.

Security boundaries include request-local ContextVars, explicit provider injection, no global tenant, owned compilation indexes, fail-closed scope checks even across caught exceptions, original supporting text/hash validation, and a preserved upstream citation-pool mapping. Each configured mode is explicit. No custom quality heuristic, threshold reduction, score-weight change or evaluation-specific rule was added.

Important differences: buffered API output rather than partial streaming; bounded single-worker development execution; operation-lifetime caches rather than shared Redis/durable resume; missing external web/SQL/wiki services reported as unavailable. These are platform/resource boundaries, not claims of complete RAGFlow product parity.
